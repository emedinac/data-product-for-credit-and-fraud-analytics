services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: customer_product
      POSTGRES_USER: customer
      POSTGRES_PASSWORD: customer
    ports:
      - "5432:5432"
    volumes:
      - customer_product_postgres:/var/lib/postgresql/data

volumes:
  customer_product_postgres:

